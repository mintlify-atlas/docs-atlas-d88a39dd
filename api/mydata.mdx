---
title: My Data
description: Export and delete user data (GDPR compliance)
---

Endpoints for managing user data export and account deletion in compliance with GDPR.

## Export user data

<ParamField path="GET /api/mydata/export" type="endpoint">
  Download all user data in JSON format
</ParamField>

Export all user data including identities, passkeys, devices, sessions, trust codes, activity logs, and OAuth authorizations. This endpoint supports GDPR data portability requirements.

**Authentication:** Required (session token)

### Response

<ResponseField name="exportedAt" type="string">
  ISO 8601 timestamp of when the export was generated
</ResponseField>

<ResponseField name="user" type="object">
  User account information
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      User ID
    </ResponseField>
    <ResponseField name="createdAt" type="string">
      Account creation timestamp
    </ResponseField>
    <ResponseField name="updatedAt" type="string">
      Last update timestamp
    </ResponseField>
    <ResponseField name="hasSecurityQuestions" type="boolean">
      Whether security questions are configured
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="identities" type="array">
  List of user identities
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Identity ID
    </ResponseField>
    <ResponseField name="displayName" type="string">
      Display name
    </ResponseField>
    <ResponseField name="handle" type="string">
      Unique handle
    </ResponseField>
    <ResponseField name="email" type="string">
      Email address (optional)
    </ResponseField>
    <ResponseField name="birthday" type="string">
      Birthday (optional)
    </ResponseField>
    <ResponseField name="avatarUrl" type="string">
      Avatar URL (optional)
    </ResponseField>
    <ResponseField name="bannerUrl" type="string">
      Banner URL (optional)
    </ResponseField>
    <ResponseField name="isPrimary" type="boolean">
      Whether this is the primary identity
    </ResponseField>
    <ResponseField name="createdAt" type="string">
      Creation timestamp
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="passkeys" type="array">
  List of registered passkeys (WebAuthn credentials)
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Passkey ID
    </ResponseField>
    <ResponseField name="name" type="string">
      Passkey name
    </ResponseField>
    <ResponseField name="deviceType" type="string">
      Device type (e.g., "computer", "phone", "tablet")
    </ResponseField>
    <ResponseField name="createdAt" type="string">
      Registration timestamp
    </ResponseField>
    <ResponseField name="lastUsedAt" type="string">
      Last authentication timestamp
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="devices" type="array">
  List of trusted devices
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Device ID
    </ResponseField>
    <ResponseField name="name" type="string">
      Device name
    </ResponseField>
    <ResponseField name="type" type="string">
      Device type
    </ResponseField>
    <ResponseField name="browser" type="string">
      Browser name
    </ResponseField>
    <ResponseField name="os" type="string">
      Operating system
    </ResponseField>
    <ResponseField name="createdAt" type="string">
      First seen timestamp
    </ResponseField>
    <ResponseField name="lastSeenAt" type="string">
      Last activity timestamp
    </ResponseField>
    <ResponseField name="isActive" type="boolean">
      Whether device is currently active
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="sessions" type="array">
  Active and expired sessions
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Session ID
    </ResponseField>
    <ResponseField name="createdAt" type="string">
      Session creation timestamp
    </ResponseField>
    <ResponseField name="expiresAt" type="string">
      Session expiration timestamp
    </ResponseField>
    <ResponseField name="ipAddress" type="string">
      IP address of session
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="trustCodes" type="array">
  Trust code metadata (codes themselves are not included for security)
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Trust code ID
    </ResponseField>
    <ResponseField name="createdAt" type="string">
      Generation timestamp
    </ResponseField>
    <ResponseField name="usedAt" type="string">
      Usage timestamp (null if unused)
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="activityLog" type="array">
  Complete activity history
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Log entry ID
    </ResponseField>
    <ResponseField name="action" type="string">
      Action type
    </ResponseField>
    <ResponseField name="details" type="object">
      Action-specific details
    </ResponseField>
    <ResponseField name="severity" type="string">
      Severity level (info, warning, danger)
    </ResponseField>
    <ResponseField name="ipAddress" type="string">
      IP address
    </ResponseField>
    <ResponseField name="createdAt" type="string">
      Timestamp
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="authorizedApps" type="array">
  OAuth app authorizations
  <Expandable title="properties">
    <ResponseField name="id" type="string">
      Authorization ID
    </ResponseField>
    <ResponseField name="appId" type="string">
      OAuth app ID
    </ResponseField>
    <ResponseField name="identityId" type="string">
      Identity ID
    </ResponseField>
    <ResponseField name="createdAt" type="string">
      Authorization timestamp
    </ResponseField>
  </Expandable>
</ResponseField>

### Example

```bash
curl https://api.aveid.net/api/mydata/export \
  -H "Cookie: ave_session=YOUR_SESSION_TOKEN"
```

**Response:**
```json
{
  "exportedAt": "2024-03-15T10:30:00.000Z",
  "user": {
    "id": "user_abc123",
    "createdAt": "2024-01-01T00:00:00.000Z",
    "updatedAt": "2024-03-15T10:30:00.000Z",
    "hasSecurityQuestions": false
  },
  "identities": [
    {
      "id": "identity_xyz789",
      "displayName": "Alice Smith",
      "handle": "alice",
      "email": "alice@example.com",
      "birthday": "1990-01-01",
      "avatarUrl": "https://...",
      "isPrimary": true,
      "createdAt": "2024-01-01T00:00:00.000Z"
    }
  ],
  "passkeys": [...],
  "devices": [...],
  "sessions": [...],
  "trustCodes": [...],
  "activityLog": [...],
  "authorizedApps": [...]
}
```

<Note>
  The export is generated on demand and includes all data at the time of the request. The file is downloaded with a filename like `ave-data-export-2024-03-15.json`.
</Note>

## Delete account

<ParamField path="DELETE /api/mydata/" type="endpoint">
  Permanently delete user account and all associated data
</ParamField>

Permanently delete the user's account and all associated data. This action is irreversible and complies with GDPR's "right to erasure" (right to be forgotten).

**Authentication:** Required (session token)

**Deleted data includes:**
- User account
- All identities
- All passkeys
- All devices
- All sessions
- All trust codes
- All activity logs
- All OAuth authorizations
- All signing keys
- Uploaded files (avatars, banners)

### Response

<ResponseField name="success" type="boolean">
  Always `true` if deletion succeeds
</ResponseField>

<ResponseField name="message" type="string">
  Confirmation message
</ResponseField>

### Example

```bash
curl -X DELETE https://api.aveid.net/api/mydata/ \
  -H "Cookie: ave_session=YOUR_SESSION_TOKEN"
```

**Response:**
```json
{
  "success": true,
  "message": "Your account and all associated data have been permanently deleted."
}
```

<Warning>
  **This action is permanent and cannot be undone.** All data will be immediately and irreversibly deleted from the database. Users should export their data first if they want to keep a copy.
</Warning>

## Privacy & Security

- **GDPR Compliance**: Both endpoints support GDPR requirements for data portability and erasure
- **Activity Logging**: Data exports and account deletions are logged before execution
- **Authentication Required**: Both endpoints require valid session authentication
- **No PII in Logs**: Trust codes and sensitive data are excluded or hashed in exports
- **Cascade Deletion**: Account deletion automatically removes all related data via database constraints

## Next Steps

<CardGroup cols={2}>
  <Card title="Activity log" icon="list" href="/api/activity">
    View activity history
  </Card>
  <Card title="Security settings" icon="shield" href="/api/security">
    Manage security options
  </Card>
</CardGroup>
